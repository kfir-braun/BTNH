---
title: Facades Without a Performance Hit
section: aesthetics
description: Dressing up machine rooms cheaply using facades instead of extra blocks.
---

Facades let you cover ugly machine casings with any block texture at
effectively zero extra hitbox/entity cost. Pick one or two "trim" blocks per
build and reuse them everywhere — visual consistency reads as intentional
even in a purely functional factory room.
